# EDU_BRIEF.md
## MacroSnaps Education Platform (macedu)
### Living brief — updated Session 17

---

## Project context

Next.js 16 App Router, statically exported, Cloudflare Pages. Live at macroeconomics.education. Repo: https://github.com/ralphlazar/macedu. Local: `/Users/lisaswerling/RALPH/AI/macedu`. Ralph is non-technical; Claude writes all code. UK English throughout; no em dashes.

---

## Architecture (locked)

**36 cards = 6 metrics × 6 countries.**

**Layer 1 (Snapshot card):** Dark navy. Flag, country name, metric label, large value, blurb, chart for the current country, country comparison strip. Same for all users and curricula. No weather icon or direction arrow on the card itself.

**Layer 2 (Lesson overlay):** White box below the card. Four numbered sections (teacher only): 01 How to use in class, 02 Weather exercise, 03 Sample questions, 04 Discussion prompts. Share button copies student URL. Student sees section 02 only (exercise, no reveal button).

**URL structure (locked):**
```
/                                              landing page (role selector, password-gated)
/teacher/[curriculum]                          teacher homepage
/teacher/[curriculum]/[metric]/[country]       teacher lesson page
/student/[curriculum]                          student homepage
/student/[curriculum]/[metric]/[country]       student lesson page
```
MVP curriculum slug: `alevel`

**Metric slugs:** `inflation`, `unemployment`, `gdp`, `interest-rates`, `exchange-rates`, `trade`
**Country slugs:** `uk`, `us`, `eurozone`, `china`, `japan`, `brazil`
**Colours:** Teacher `#378ADD` (blue), Student `#F0843C` (orange)
**Password:** croc

---

## Data architecture

### `app/data/metrics.js`
Named export `{ metrics }`. Keyed by metric slug. Written by `sync_edu.py` (pipeline). **Never edited by hand.**

Structure per country entry:
```js
metrics['inflation']['uk'] = {
  flag, name, value, direction, releasedDaysAgo,
  icon,        // editorial: ☀️ ☁️ ⛈️
  reveal,      // editorial: judgment text shown on teacher reveal
  blurb,       // editorial: UK only currently; others empty string
  chartDates,  // array of date strings
  chartSeries, // array of numeric values
}
```

### `app/data/aqa-alevel.js`
Named export `{ lesson }`. Keyed by metric slug. **Edited by hand.** Never touched by pipeline.

Structure per metric:
```js
lesson['inflation'] = {
  exercisePrompt, exerciseOptions,
  classroomTime: { five, ten, twenty },
  sampleQuestions: [{ q, hint }, ...],
  discussionPrompts: [...],
}
```

### `app/data/upcomingReleases.js`
Copied from old repo. Used by TeacherHomePage sidebar. Not generated by pipeline currently.

---

## Component map

| File | Role |
|------|------|
| `AnnotatedChart.js` | Recharts LineChart. Props: `dates, series, target, color, height, dark`. x-ticks: all ticks when ≤15 pts, else every 12th. |
| `CountryComparison.js` | Sorted high-to-low strip. Props: `metric, currentCountry, snapshots`. Uses `snap.name \|\| snap.country`. |
| `InflationChart.js` | Thin wrapper: `<AnnotatedChart dark target={2} />` |
| `UnemploymentChart.js` | Thin wrapper: `<AnnotatedChart dark />` |
| `GdpChart.js` | Thin wrapper: `<AnnotatedChart dark />` |
| `InterestRatesChart.js` | Thin wrapper: `<AnnotatedChart dark />` |
| `ExchangeRatesChart.js` | Thin wrapper: `<AnnotatedChart dark />` |
| `TradeChart.js` | Thin wrapper: `<AnnotatedChart dark />` |
| `Header.js` | Role stripe (blue/orange top border), site name, Switch role link. Props: `role, homeHref`. |
| `SnapshotCard.js` | Layer 1. Dark navy card. Props: `metric, country, data, aqaRef, metricTitle, allCountries`. |
| `LessonOverlay.js` | Layer 2. White box. Props: `metric, country, lessonData, reveal, curriculum, showReveal, studentMode`. |
| `MetricCard.js` | Homepage grid card. Props: `metricSlug, metricTitle, aqaRef, countries, role, curriculum`. |
| `TeacherHomePage.js` | Release feed (24h/7d/30d toggle) + upcoming sidebar. Props: `metrics, curriculum`. |
| `StudentHomePage.js` | Orange title bar + 2×3 MetricCard grid. Props: `metrics, curriculum`. |
| `PasswordGate.js` | Copied from old repo. Wraps all pages via `app/layout.js`. Password: croc. |

---

## Route files

All routes live under `app/` (no `alevel/(unlocked)/` route group).

| Route | File |
|-------|------|
| `/` | `app/page.js` |
| `/teacher/[curriculum]` | `app/teacher/[curriculum]/page.js` |
| `/teacher/[curriculum]/[metric]/[country]` | `app/teacher/[curriculum]/[metric]/[country]/page.js` |
| `/student/[curriculum]` | `app/student/[curriculum]/page.js` |
| `/student/[curriculum]/[metric]/[country]` | `app/student/[curriculum]/[metric]/[country]/page.js` |

All dynamic routes have `generateStaticParams`. All params are awaited.

---

## Typography

- Display/headings: Instrument Serif (loaded via `<link>` in `app/layout.js` — NOT `next/font/google`)
- Body/UI: IBM Plex Sans
- Data values: IBM Plex Mono

---

## Key technical rules

- **Static export only.** No `cookies()`, no middleware, no server-side features.
- **Python scripts for all file writes.** Never bash heredocs (mangle JSX).
- **`metrics.js` never edited by hand.** Pipeline writes it exclusively.
- **Emoji written directly in Python source.** Never as Unicode escape sequences.
- **Patch scripts are disposable.** Run once, then deleted. Never committed.
- **`app/data/` for data layer; `app/components/` for all components.**
- **Local preview before every push.**
- **Three weather icons only:** ☀️ ☁️ ⛈️

---

## Session history

| Session | Summary |
|---------|---------|
| 1--14 | Iterative build of old architecture (`alevel/(unlocked)/` route group, `edu-data.json`, `concepts.js`). LessonPage, TeacherHomePage, StudentLessonPage built and refined. |
| 15 | Student lesson page at `/alevel/student/concepts/[concept]/[country]`. Weather exercise with icon select. |
| 16 | Header redesign. Various fixes. |
| 17 | **Blank-slate rewrite.** Old repo wiped. New architecture: two-layer model (SnapshotCard + LessonOverlay), new URL structure (`/teacher/[curriculum]/...`), new data layer (`metrics.js` + `aqa-alevel.js`). 78 static pages. Build clean. Deployed. |

---

## On the horizon

- **Next session:** Visual design pass. Review all four page types at localhost against Bloomberg principle. Identify what needs improving.
- **sync_edu.py update:** Rewrite to write `metrics.js` (new format) instead of `edu-data.json`.
- **Glossary layer:** 300--400 terms, two diagram renderer types (curve renderer, flow diagram renderer).
- **Blurbs:** Currently only UK has blurbs. Pipeline-based blurb generation for all 36 cards.
- **Upcoming curricula:** Cambridge IGCSE, India CBSE (strategic priority at scale).
- **Long-term:** Multilingual expansion, mobile app, data layer API, AI-assisted live assessment.
